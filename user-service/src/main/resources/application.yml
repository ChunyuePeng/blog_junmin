server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://20.1.0.206:3306/test_db?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: SW#cfg1506
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    baseline-on-migrate: true
    baseline-version: 1.0
    clean-disabled: true
    enabled: true
    out-of-order: true
    locations: classpath:db.migration

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.junmin.entity
  configuration:
    map-underscore-to-camel-case: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka

logging:
  level:
    org.mybatis: DEBUG